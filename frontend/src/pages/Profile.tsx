import React from "react";
import { Container, Typography } from "@mui/material";

const Profile = () => {
  return (
    <Container>
      <Typography variant="h3">Trang Cá Nhân</Typography>
    </Container>
  );
};
export default Profile;